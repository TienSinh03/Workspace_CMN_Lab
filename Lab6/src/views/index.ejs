<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="index.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <title>Lab2</title>
</head>

<body>
    <div class="container">
        <div class="main">
            <h1 class="text">Danh Sách Sản Phẩm</h1>
            <!-- form input -->
            <form action="/" method="POST">
                <div class="mb-3">
                    <label for="Id" class="form-label">ID sản phẩm</label>
                    <input type="text" class="form-control" name="Id" required>
                </div>
                <div class="mb-3">
                    <label for="product_name" class="form-label">Tên sản phẩm</label>
                    <input type="text" class="form-control" name="product_name" required>
                </div>
                <div class="mb-3">
                    <label for="quantity" class="form-label">Số lượng</label>
                    <input type="number" class="form-control" name="quantity" required>
                </div>
                <button type="submit" class="btn btn-primary">Thêm sản phẩm</button>
            </form>
            <form id="delete" method="POST" action="/delete" enctype="multipart/form-data">
                <table class="table">
                    <thead>
                        <tr>
                            <th>STT</th>
                            <th>Mã SP</th>
                            <th>Tên SP</th>
                            <th>Số Lượng</th>
                            <th>Hình ảnh</th>
                            <th>Chọn</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% for (let i = 0; i < sanPhams.length; i++) { %>
                            <tr>
                                <td><%= i + 1 %></td>
                                <td><%= sanPhams[i].Id %></td>
                                <td><%= sanPhams[i].product_name %></td>
                                <td><%= sanPhams[i].quantity %></td>
                                <td></td>
                                <td>
                                    <input type="checkbox" name="chon[]" value="<%= sanPhams[i].Id %>">
                                </td> 
                            </tr>
                        <% } %>
                    </tbody>
                </table>
                <button type="submit" class="btn btn-danger">Xóa sản phẩm</button>
            </form>
            
        </div>
    </div>
</body>

</html>
